# .github/workflows/swift-tests.yml
name: Swift Tests

on:
  workflow_dispatch: # TODO: remove this line after having succesfully tested the workflow
  pull_request:
    branches: [unit_tests_on_PR] # `main` or 'master' or your default branch
  push:
    branches: [unit_tests_on_PR]

jobs:
  test:
    name: Run Unit Tests
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.3.app # adjust to your target version

      - name: Build and test
        run: |
          xcodebuild test \
            -scheme Trio \
            -sdk iphonesimulator \
            -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.4' \
            -enableCodeCoverage YES

      - name: Upload test results (optional)
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: xcode-test-logs
          path: ~/Library/Logs/DiagnosticReports # or wherever your logs are generated