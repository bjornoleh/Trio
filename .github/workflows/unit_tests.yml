name: zzz [DO NOT RUN] Automated unit tests

on:
  workflow_dispatch: # TODO: remove this after testing
  pull_request:
    branches: [trio_tests_ci_no_scheme_changes]
  push:
    branches: [trio_tests_ci_no_scheme_changes]

jobs:
  test:
    name: Run Unit Tests
    runs-on: macos-15
    #if: github.repository_owner == 'nightscout' TODO: include this if-statement after testing

    steps:
      - name: Select Xcode version
        run: sudo xcode-select -s /Applications/Xcode_16.3.app/Contents/Developer

      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build for testing
        run: |
          set -o pipefail && \
          time xcodebuild build-for-testing \
            -workspace Trio.xcworkspace \
            -scheme "Trio Tests" \
            -destination 'platform=iOS Simulator,name=iPhone 16,OS=18.4' \

      - name: Run tests and save log
        run: |
          set -o pipefail
          time xcodebuild test-without-building \
            -workspace Trio.xcworkspace \
            -scheme "Trio Tests" \
            -destination 'platform=iOS Simulator,name=iPhone 16,OS=18.4' \
            -resultBundlePath TestResults.xcresult \
          2>&1 | tee xcodebuild.log
        continue-on-error: true

      - name: Annotate test results
        if: always()
        run: |
          if [ -f xcodebuild.log ]; then
            if grep -q "Failing tests:" xcodebuild.log; then
              echo "::error title=Unit Tests Failed::Some tests failed"
              echo "## ❌ Some tests failed:" >> $GITHUB_STEP_SUMMARY

              grep -A 20 "Failing tests:" xcodebuild.log | \
                grep -E '^\s+[A-Za-z0-9]+\..+\(\)' | \
                sed 's/^/  - /' >> $GITHUB_STEP_SUMMARY

              echo "::group::Failed Test List"
              grep -A 20 "Failing tests:" xcodebuild.log | \
                grep -E '^\s+[A-Za-z0-9]+\..+\(\)' | \
                sed 's/^/  - /'
              echo "::endgroup::"
            else
              echo "::notice title=Unit Tests Passed::✅ All tests passed"
              echo "✅ All tests passed" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "::warning::Test log (xcodebuild.log) not found"
          fi

      - name: Install xchtmlreport
        run: |
          brew install xchtmlreport
  
      - name: Generate test reports (JUnit & HTML)
        run: |
          mkdir -p test-results/html
          xchtmlreport -r TestResults.xcresult -j test-results/junit.xml -o test-results/html

      - name: Publish test results
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: Unit Tests
          path: test-results/junit.xml
          reporter: java-junit

      - name: Upload test artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: xcode-test-artifacts
          path: |
            xcodebuild.log
            TestResults.xcresult
            test-results

      - name: Annotate summary
        if: always()
        run: |
          if [ -f test-results/junit.xml ]; then
            total=$(grep -o 'testsuite tests="[0-9]*"' test-results/junit.xml | sed 's/[^0-9]*//g')
            failures=$(grep -o 'failures="[0-9]*"' test-results/junit.xml | sed 's/[^0-9]*//g')
            echo "## 🧪 Unit Test Summary" >> $GITHUB_STEP_SUMMARY
            echo "**Total tests:** $total" >> $GITHUB_STEP_SUMMARY
            echo "**Failures:** $failures" >> $GITHUB_STEP_SUMMARY
          else
            echo "::warning::JUnit summary skipped — no XML file found"
          fi